# Voyalier contracts

Versioned transport and domain contracts shared by the web, local API, fixtures, and future integrations.

The Rust domain model remains authoritative. Contract generation will replace the small hand-maintained TypeScript surface before the first public beta.
